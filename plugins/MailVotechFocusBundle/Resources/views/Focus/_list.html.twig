{#
#}
{{ includeScript('plugins/MailVotechFocusBundle/Assets/js/focus.js') }}
{% if items|length > 0 %}
    <div class="table-responsive page-list">
        <table class="table table-hover focus-list" id="focusTable">
            <thead>
            <tr>
                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                    'checkall': 'true',
                    'target': '#focusTable',
                }) }}

                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'focus',
                        'orderBy': 'f.name',
                        'text': 'mailvotech.core.name',
                        'class': 'col-focus-name',
                        'default': true,
                }) }}

                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'focus',
                        'orderBy': 'c.title',
                        'text': 'mailvotech.core.category',
                        'class': 'visible-md visible-lg col-focus-category',
                }) }}

                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'focus',
                        'orderBy': 'f.type',
                        'text': 'mailvotech.focus.thead.type',
                        'class': 'visible-md visible-lg col-focus-type',
                }) }}

                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'focus',
                        'orderBy': 'f.style',
                        'text': 'mailvotech.focus.thead.style',
                        'class': 'visible-md visible-lg col-focus-style',
                }) }}

                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'focus',
                        'orderBy': 'f.id',
                        'text': 'mailvotech.core.id',
                        'class': 'visible-md visible-lg col-focus-id',
                }) }}
            </tr>
            </thead>
            <tbody>
            {% for item in items %}
                <tr>
                    <td>
                        {{ include('@MailVotechCore/Helper/list_actions.html.twig', {
                                'item': item,
                                'templateButtons': {
                                    'edit': securityHasEntityAccess(permissions['focus:items:editown'], permissions['focus:items:editother'], item.createdBy),
                                    'clone': permissions['focus:items:create'],
                                    'delete': securityHasEntityAccess(permissions['focus:items:deleteown'], permissions['focus:items:deleteother'], item.createdBy),
                                },
                                'routeBase': 'focus',
                        }) }}
                    </td>
                    <td>
                        <div>
                            {{ include('@MailVotechCore/Helper/publishstatus_icon.html.twig', {'item': item, 'model': 'focus'}) }}
                            <a data-toggle="ajax" href="{{ path('mailvotech_focus_action', {'objectId': item.id, 'objectAction': 'view'}) }}">
                                {{ item.name }}
                            </a>
                            {{ include('@MailVotechProject/Modules/projects.html.twig') }}
                        </div>
                        {{ include('@MailVotechCore/Helper/description--inline.html.twig', {
                            'description': item.description
                        }) }}
                    </td>
                    <td class="visible-md visible-lg">
                        {{ include('@MailVotechCore/Modules/category--expanded.html.twig', {'category': item.category}) }}
                    </td>
                    <td class="visible-md visible-lg">{{ ('mailvotech.focus.type.' ~ item.type)|trans }}</td>
                    <td class="visible-md visible-lg">{{ ('mailvotech.focus.style.' ~ item.style)|trans }}</td>
                    <td class="visible-md visible-lg">{{ item.id }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    <div class="panel-footer">
        {{ include('@MailVotechCore/Helper/pagination.html.twig', {
                'totalItems': items|length,
                'page': page,
                'limit': limit,
                'baseUrl': path('mailvotech_focus_index'),
                'sessionVar': 'focus',
        }) }}
    </div>
{% else %}
    {% if searchValue is not empty %}
        {{ include('@MailVotechCore/Helper/noresults.html.twig', {'tip': 'mailvotech.focus.noresults.tip'}) }}
    {% else %}
        <div class="mt-80 col-md-offset-2 col-lg-offset-3 col-md-8 col-lg-5 height-auto">
            {% set childContainer %}
                <div class="mb-md">
                    {% include '@MailVotechCore/Components/pictogram.html.twig' with {
                        'pictogram': 'spotlight',
                        'size': '80'
                    } %}
                </div>

                {{ include('@MailVotechCore/Components/content-item-row.html.twig', {
                    type: 'default',
                    eyebrow: 'mailvotech.focus.onboarding.eyebrow',
                    heading: 'mailvotech.focus.onboarding.purpose.heading',
                    copy: 'mailvotech.focus.onboarding.purpose.copy',
                }) }}
                
                {% set focusFeaturesContainer %}
                <div class="row">
                    <div class="col-sm-6 col-xs-12">
                        {{ include('@MailVotechCore/Components/content-item.html.twig', {
                            type: 'pictogram',
                            pictogram: 'web--banners',
                            heading: 'mailvotech.focus.onboarding.style.bar.heading',
                            copy: 'mailvotech.focus.onboarding.style.bar.copy',
                        }) }}
                    </div>
                    <div class="col-sm-6 col-xs-12">
                        {{ include('@MailVotechCore/Components/content-item.html.twig', {
                            type: 'pictogram',
                            pictogram: 'design-and-development--01',
                            heading: 'mailvotech.focus.onboarding.style.modal.heading',
                            copy: 'mailvotech.focus.onboarding.style.modal.copy',
                        }) }}
                    </div>
                    <div class="col-sm-6 col-xs-12">
                        {{ include('@MailVotechCore/Components/content-item.html.twig', {
                            type: 'pictogram',
                            pictogram: 'notifications',
                            heading: 'mailvotech.focus.onboarding.style.notification.heading',
                            copy: 'mailvotech.focus.onboarding.style.notification.copy',
                        }) }}
                    </div>
                    <div class="col-sm-6 col-xs-12">
                        {{ include('@MailVotechCore/Components/content-item.html.twig', {
                            type: 'pictogram',
                            pictogram: 'maximize',
                            heading: 'mailvotech.focus.onboarding.style.fullpage.heading',
                            copy: 'mailvotech.focus.onboarding.style.fullpage.copy',
                        }) }}
                    </div>
                </div>
                {% endset %}

                {{ include('@MailVotechCore/Components/content-group.html.twig', {
                    heading: 'mailvotech.focus.onboarding.styles.heading',
                    childContainer: focusFeaturesContainer,
                }) }}
                {% endset %}
                
                {{ include('@MailVotechCore/Components/content-block.html.twig', {
                    heading: 'mailvotech.focus.onboarding.heading',
                    subheading: 'mailvotech.focus.onboarding.subheading',
                    copy: '',
                childContainer: childContainer,
            }) }}
        </div>
    {% endif %}
{% endif %}
