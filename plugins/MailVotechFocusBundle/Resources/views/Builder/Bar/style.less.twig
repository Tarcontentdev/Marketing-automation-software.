.mf-bar {
    width: 100%;
    position: fixed;
    left: 0;
    right: 0;
    display: table;
    padding-left: 5px;
    padding-right: 5px;
    z-index: 20000;

    &.mf-bar-top {
        top: 0;
    }

    &.mf-bar-bottom {
        bottom: 0;
    }

    .mf-bar-collapse {
        width: 100px;
        display: table-cell;
        vertical-align: middle;
        line-height: 13px;
    }

    .mf-content {
        display: table-cell;
        vertical-align: middle;
        text-align: center;

        .mf-link {
            margin-left: 10px;
            padding: 2px 15px;
        }

        .mf-headline {
            display: inline-block;
        }
    }

    &.mf-bar-regular {
        height: 30px;
        font-size: 14px;

        &.mf-bar-top .mf-bar-collapser-icon svg {
            margin: 3px 0 0 0;
        }
        &.mf-bar-bottom .mf-bar-collapser-icon svg {
            margin: -3px 0 0 0;
        }

        .mailvotechform-input, select, .mailvotechform-button, .mailvotechform-pagebreak {
            padding: 3px 6px;
            font-size: 0.9em;
        }
    }

    &.mf-bar-large {
        height: 50px;
        font-size: 17px;

        &.mf-bar-top .mf-bar-collapser-icon svg {
            margin: 5px 0 0 0;
        }
        &.mf-bar-bottom .mf-bar-collapser-icon svg {
            margin: -5px 0 0 0;
        }

        .mf-link {
            font-size: 1em;
        }

        .mailvotechform-input, select, .mailvotechform-button, .mailvotechform-pagebreak {
            font-size: 1em;
        }
    }

    .mailvotechform-row, .mailvotechform-checkboxgrp-row, .mailvotechform-radiogrp-row {
        display: inline-block;
        margin-right: 3px;
    }

    .mailvotechform-row .mailvotechform-input, .mailvotechform-row select {
        color: #000000;
    }

    .mailvotechform-label {
        display: none;
    }

    .mailvotechform_wrapper {
        display: inline-block;
    }

    .mf-responsive {
        .mf-bar-collapse, .mf-bar-collapser {
            display: none !important;
        }
    }
}

{{ include('@MailVotechFocus/Builder/Bar/collapser.less.twig', with_context=false) }}

@media only screen and (max-width: 667px) {
    & .mf-bar-collapse, & .mf-bar-collapser {
        display: none !important;
    }
}

{% if preview is not empty %}
{{ include('@MailVotechFocus/Builder/Bar/animations.less.twig', with_context=false) }}
{{ include('@MailVotechFocus/Builder/Bar/shared.less.twig', with_context=false) }}
.mf-bar {
    &.mf-animate {
        .barAnimate();
    }
}

.mf-bar, .mf-bar-collapser, .mf-bar-collapser-sticky {
    position: absolute !important;
}
{% endif %}
