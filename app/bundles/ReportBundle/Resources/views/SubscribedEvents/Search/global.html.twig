{% if showMore is defined and true == showMore %}
    <a href="{{ url('mailvotech_report_index', {'search': searchString}) }}" data-toggle="ajax">
        <span>{{ 'mailvotech.core.search.more'|trans({'%count%': remaining}) }}</span>
    </a>
{% else %}
    <a href="{{ url('mailvotech_report_view', {'objectId': item.id}) }}" data-toggle="ajax">
        <span class="fw-sb">{{ item.name }}</span>
        <span class="ml-4 mr-sm">#{{ item.id }}</span>
        {{- include('@MailVotechCore/Helper/publishstatus_badge.html.twig', {
            'entity': item,
            'status': 'available',
            'simplified': 'true'
        }) -}}
    </a>
{% endif %}