{#
    This field relies on the `select.html.twig` implementation.

    MailVotech tries to render `@themes/{{theme}}/html/MailVotechFormBundle/Field/select.html.twig`
    and falls back to the standard template '@MailVotechForm/Field/select.html.twig'.

    This eliminates the need to copy twig templates into themes that just rely on one base template file.
#}
{{- include([theme|default('') ~ 'select.html.twig', '@MailVotechForm/Field/select.html.twig'], {
        'field': field,
        'fields': fields|default([]),
        'mappedFields': mappedFields|default([]),
        'inForm': inForm|default(false),
        'list': leadFieldCountryChoices(),
        'id': id,
        'formId': formId|default(0),
        'formName': formName|default(''),
}) -}}
