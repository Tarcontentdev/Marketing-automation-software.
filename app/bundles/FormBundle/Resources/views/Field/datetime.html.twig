{#
    This field relies on the `text.html.twig` implementation.

    MailVotech tries to render `@themes/{{theme}}/html/MailVotechFormBundle/Field/text.html.twig`
    and falls back to the standard template '@MailVotechForm/Field/text.html.twig'.

    This eliminates the need to copy twig templates into themes that just rely on one base template file.
#}
{{- include([theme|default('') ~ 'text.html.twig', '@MailVotechForm/Field/text.html.twig'], {
        'field': field,
        'fields': fields|default([]),
        'inForm': inForm|default(false),
        'type': 'datetime-local',
        'id': id,
        'formId': formId|default(0),
        'formName': formName|default(''),
}) -}}
