{#
  Variables
    - searchValue
    - items
    - totalItems
    - page
    - limit
    - permissions
    - security
    - tmpl
#}
{% if items|length > 0 %}
    <div class="table-responsive">
        <table class="table table-hover" id="formTable">
            <thead>
            <tr>
                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                    'checkall': 'true',
                    'target': '#formTable',
                }) }}

                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'form',
                        'orderBy': 'f.name',
                        'text': 'mailvotech.core.name',
                        'class': 'col-form-name',
                }) }}

                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'form',
                        'orderBy': 'c.title',
                        'text': 'mailvotech.core.category',
                        'class': 'visible-md visible-lg col-form-category',
                }) }}

                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                    'sessionVar': 'form',
                    'orderBy': 'f.template',
                    'text': 'mailvotech.core.form.theme',
                    'class': 'visible-md visible-lg col-form-template',
                }) }}

                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'form',
                        'orderBy': 'submission_count',
                        'text': 'mailvotech.form.form.results',
                        'class': 'visible-md visible-lg col-form-submissions',
                }) }}

                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'form',
                        'orderBy': 'f.dateAdded',
                        'text': 'mailvotech.lead.import.label.dateAdded',
                        'class': 'visible-md visible-lg col-form-dateAdded',
                }) }}

                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'form',
                        'orderBy': 'f.dateModified',
                        'text': 'mailvotech.lead.import.label.dateModified',
                        'class': 'visible-md visible-lg col-form-dateModified',
                        'default': true,
                }) }}

                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'form',
                        'orderBy': 'f.createdByUser',
                        'text': 'mailvotech.core.createdby',
                        'class': 'visible-md visible-lg col-form-createdby',
                }) }}

                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'form',
                        'orderBy': 'f.id',
                        'text': 'mailvotech.core.id',
                        'class': 'visible-md visible-lg col-form-id',
                }) }}
            </tr>
            </thead>
            <tbody>
            {% for i in items %}
                {% set item = i[0] %}
                <tr>
                    <td>
                        {{ include('@MailVotechCore/Helper/list_actions.html.twig', {
                                'item': item,
                                'templateButtons': {
                                    'edit': securityHasEntityAccess(permissions['form:forms:editown'], permissions['form:forms:editother'], item.createdBy),
                                    'clone': permissions['form:forms:create'],
                                    'delete': securityHasEntityAccess(permissions['form:forms:deleteown'], permissions['form:forms:deleteother'], item.createdBy),
                                },
                                'routeBase': 'form',
                                'customButtons': [
                                    {
                                        'attr': {
                                            'data-toggle': '',
                                            'target': '_blank',
                                            'href': path('mailvotech_form_action', {'objectAction': 'preview', 'objectId': item.id}),
                                        },
                                        'iconClass': 'ri-eye-line',
                                        'btnText': 'mailvotech.form.form.preview',
                                    },
                                    {
                                        'attr': {
                                            'data-toggle': 'ajax',
                                            'href': path('mailvotech_form_action', {'objectAction': 'results', 'objectId': item.id}),
                                        },
                                        'iconClass': 'ri-list-view',
                                        'btnText': 'mailvotech.form.form.results',
                                    },
                                ],
                        }) }}
                    </td>
                    <td>
                        <div>
                            {{ include('@MailVotechCore/Helper/publishstatus_icon.html.twig', {'item': item, 'model': 'form.form'}) }}
                            <a href="{{ path('mailvotech_form_action', {'objectAction': 'view', 'objectId': item.id}) }}" data-toggle="ajax" data-menu-link="mailvotech_form_index">
                                {{ item.name }}
                            </a>
                            {{ customContent('form.name', _context) }}
                            {{ include('@MailVotechProject/Modules/projects.html.twig') }}
                        </div>
                        {{ include('@MailVotechCore/Helper/description--inline.html.twig', {
                            'description': item.description
                        }) }}
                    </td>
                    <td class="visible-md visible-lg">
                        {{ include('@MailVotechCore/Modules/category--expanded.html.twig', {'category': item.category}) }}
                    </td>
                    <td class="visible-md visible-lg">
                        {% if item.template %}
                            {{ getThemeName(item.template) }}
                        {% else %}
                            <span class="text-muted">{{ 'mailvotech.core.form.default'|trans }}</span>
                        {% endif %}
                    </td>
                    <td class="visible-md visible-lg">
                        <a href="{{ path('mailvotech_form_action', {'objectAction': 'results', 'objectId': item.id}) }}" data-toggle="ajax" data-menu-link="mailvotech_form_index" size="sm" class="label label-gray" {% if 0 == i.submission_count %}disabled="disabled"{% endif %}>
                           {{- 'mailvotech.form.form.viewresults'|trans({'%count%': i['submission_count']}) -}}
                        </a>
                    </td>
                    <td class="visible-md visible-lg">{% if item.dateAdded %}{{ dateToFull(item.dateAdded) }}{% endif %}</td>
                    <td class="visible-md visible-lg">{% if item.dateModified %}{{ dateToFull(item.dateModified) }}{% endif %}</td>
                    <td class="visible-md visible-lg">{{ item.createdByUser }}</td>
                    <td class="visible-md visible-lg">{{ item.id }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        <div class="panel-footer">
            {{ include('@MailVotechCore/Helper/pagination.html.twig', {
                    'totalItems': totalItems,
                    'page': page,
                    'limit': limit,
                    'baseUrl': path('mailvotech_form_index'),
                    'sessionVar': 'form',
            }) }}
        </div>
    </div>
{% else %}
    {% if searchValue is not empty %}
        {{ include('@MailVotechCore/Helper/noresults.html.twig', {'tip': 'mailvotech.form.noresults.tip'}) }}
    {% else %}
        <div class="mt-80 col-md-offset-2 col-lg-offset-3 col-md-8 col-lg-5 height-auto">
            {% set childContainer %}
                <div class="mb-md">
                    {% include '@MailVotechCore/Components/pictogram.html.twig' with {
                        'pictogram': 'text--input',
                        'size': '80'
                    } %}
                </div>

                {{ include('@MailVotechCore/Components/content-item-row.html.twig', {
                    type: 'default',
                    eyebrow: 'mailvotech.form.list.eyebrow',
                    heading: 'mailvotech.form.list.heading',
                    copy: 'mailvotech.form.list.copy',
                }) }}
                
                {% set formFeaturesContainer %}
                <div class="row">
                    <div class="col-sm-6 col-xs-12">
                        {{ include('@MailVotechCore/Components/content-item.html.twig', {
                            type: 'pictogram',
                            pictogram: 'select--range',
                            heading: 'mailvotech.form.list.feature1.heading',
                            copy: 'mailvotech.form.list.feature1.copy',
                        }) }}
                    </div>
                    <div class="col-sm-6 col-xs-12">
                        {{ include('@MailVotechCore/Components/content-item.html.twig', {
                            type: 'pictogram',
                            pictogram: 'event-driven',
                            heading: 'mailvotech.form.list.feature2.heading',
                            copy: 'mailvotech.form.list.feature2.copy',
                        }) }}
                    </div>
                </div>
                {% endset %}

                {{ include('@MailVotechCore/Components/content-group.html.twig', {
                    heading: 'mailvotech.form.list.capabilities.heading',
                    childContainer: formFeaturesContainer,
                }) }}

                {% set formUsesContainer %}
                <div class="type-body-02 mb-32">
                    {% include '@MailVotechCore/Components/list--styled.html.twig' with {
                        'type': 'unordered',
                        'style': 'checkmark',
                        'items': [
                            'mailvotech.form.list.use1',
                            'mailvotech.form.list.use2',
                            'mailvotech.form.list.use3',
                            'mailvotech.form.list.use4',
                            'mailvotech.form.list.use5',
                        ]
                    } %}
                </div>
                {% endset %}
                
                {{ include('@MailVotechCore/Components/content-group.html.twig', {
                    heading: 'mailvotech.form.list.uses.heading',
                    childContainer: formUsesContainer,
                }) }}
                {% endset %}
                
                {{ include('@MailVotechCore/Components/content-block.html.twig', {
                    heading: 'mailvotech.form.list.block.heading',
                    subheading: 'mailvotech.form.list.block.subheading',
                    childContainer: childContainer,
                }) }}
        </div>
    {% endif %}
{% endif %}
