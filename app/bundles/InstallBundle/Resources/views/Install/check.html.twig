{#
    @copyright   2014 MailVotech Contributors. All rights reserved
    @author      MailVotech
    @link        http://mailvotech.org
    @license     GNU/GPLv3 http://www.gnu.org/licenses/gpl-3.0.html
#}

{#use MailVotech\InstallBundle\Configurator\Step\CheckStep;#}

{% if 'index' == tmpl %}
    {% extends '@MailVotechInstall/Install/content.html.twig' %}
{% endif %}

{% block content %}
<div class="panel-heading">
    <h2 class="panel-title">
        {{ 'mailvotech.install.heading.check.environment'|trans }}
    </h2>
</div>
{% autoescape false %}
<div class="panel-body">
    {% if majors|length %}
    <div class="panel-group" id="minors">
        <div class="panel panel-danger">
            <div class="panel-heading">
                <h3 class="panel-title" style="font-size: 1em;">
                    {{ 'mailvotech.install.heading.major.problems'|trans }}
                </h3>
            </div>
            <div class="panel-body alert-warning">
                {{ 'mailvotech.install.sentence.major.problems'|trans ({'%majors%' : majors|length}) }}
            </div>
            <ul class="list-group">
                {% for message in majors %}
                    {% if message == 'mailvotech.install.cache.unwritable' %}
                        <li class="list-group-item">{{ 'mailvotech.install.directory.unwritable'|trans ({'%path%' : cacheDir}) }}</li>
                    {% elseif message == 'mailvotech.install.config.unwritable' %}
                        <li class="list-group-item">{{ message|trans ({'%path%' : configFile}) }}</li>
                    {% elseif message == 'mailvotech.install.logs.unwritable' %}
                        <li class="list-group-item">{{ 'mailvotech.install.directory.unwritable'|trans ({'%path%' : logDir}) }}</li>
                    {% elseif message == 'mailvotech.install.apc.version' %}
                        {% set minAPCverison = (constant('PHP_VERSION') >= '5.4.0') ? '3.1.13' : '3.0.17' %}
                        <li class="list-group-item">{{ message|trans ({'%minapc%' : minAPCverison, '%currentapc%' : 'apc'|phpversion}) }}</li>
                    {% else %}
                        <li class="list-group-item">{{ message|trans }}</li>
                    {% endif %}
                {% endfor %}
            </ul>
        </div>
    </div>
    {% endif %}

    {% if not (majors|length) %}
        <div class="alert alert-success">
            <h4><i class="ri-check-line"></i> {{ 'mailvotech.install.heading.ready'|trans }}</h4>
            <p>{{ 'mailvotech.install.sentence.ready'|trans }}</p>
        </div>
    {% endif %}

    {% if minors|length %}
    <div class="panel-group" id="minors">
        <div class="panel panel-warning">
            <div class="panel-heading">
                <h3 class="panel-title" style="font-size: 1em;">
                    <a data-toggle="collapse" style="display: block; " href="#minorDetails"><i class="ri-arrow-down-s-line"></i> {{ 'mailvotech.install.heading.minor.problems'|trans }}</a>
                </h3>
            </div>
            <div id="minorDetails" class="panel-collapse collapse">
                <div class="panel-body alert-warning">
                    <p>{{ 'mailvotech.install.sentence.minor.problems'|trans }}</p>
                </div>
                 <ul class="list-group">
                     {% for message in minors %}
                         {% if message == 'mailvotech.install.pcre.version' %}
                            <li class="list-group-item">{{ message|trans ({'%pcreversion%' : constant('PCRE_VERSION')}) }}</li>
                         {% elseif message == 'mailvotech.install.php.version.not.supported' %}
                             <li class="list-group-item">{{ message|trans ({'%phpversion%' : constant('PHP_VERSION')}) }}</li>
                         {% elseif message == 'mailvotech.install.php.version.has.only.security.support' %}
                             <li class="list-group-item">{{ message|trans ({'%phpversion%' : constant('PHP_VERSION')}) }}</li>
                         {% elseif message == 'mailvotech.install.memory.limit' %}
                             <li class="list-group-item">{{ message|trans ({'%min_memory_limit%' : constant('MailVotech\\InstallBundle\\Configurator\\Step\\CheckStep::RECOMMENDED_MEMORY_LIMIT')}) }}</li>
                         {% else %}
                             <li class="list-group-item">{{ message|trans }}</li>
                         {% endif %}
                     {% endfor %}
                </ul>
            </div>
        </div>
    </div>
    {% endif %}

    {% if not (majors|length) %}
    {{ form_start(form) }}

        <div class="row mt-20">
            <div class="col-sm-12">
                {{ form_row(form.buttons) }}
            </div>
        </div>
    {{ form_end(form) }}
    {% endif %}
</div>
{% endautoescape %}
{% endblock %}