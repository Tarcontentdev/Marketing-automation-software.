{% if showMore is defined and showMore is not empty %}
    <a href="{{ url('mailvotech_stage_index', {'search' : searchString}) }}" data-toggle="ajax">
        <span>{{ 'mailvotech.core.search.more'|trans({'%count%' : remaining}) }}</span>
    </a>
    </div>
{% else %}
    <a href="{{ url('mailvotech_stage_action', {'objectAction' : 'edit', 'objectId' : item.getId()}) }}">
        <span class="fw-sb">{{ item.name }}</span>
        <span class="ml-4 mr-sm">#{{ item.getId() }}</span>
        {{- include('@MailVotechCore/Helper/publishstatus_badge.html.twig', {
            'entity': item,
            'status': 'active',
            'simplified': 'true'
        }) -}}
    </a>
    <div class="clearfix"></div>
{% endif %}