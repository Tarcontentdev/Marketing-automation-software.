{% if items|length > 0 %}
    <div class="table-responsive">
        <table class="table table-hover pointtrigger-list" id="triggerTable">
            <thead>
            <tr>
                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                    'checkall': 'true',
                    'target': '#triggerTable',
                }) }}

                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'point.trigger',
                        'orderBy': 't.name',
                        'text': 'mailvotech.core.name',
                        'class': 'col-pointtrigger-name',
                        'default': true,
                }) }}

                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'point.trigger',
                        'orderBy': 'cat.title',
                        'text': 'mailvotech.core.category',
                        'class': 'col-pointtrigger-category visible-md visible-lg',
                }) }}

                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'point.trigger',
                        'orderBy': 'pl.name',
                        'text': 'mailvotech.point.thead.group',
                        'class': 'visible-md visible-lg col-point-group',
                }) }}

                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'point.trigger',
                        'orderBy': 't.points',
                        'text': 'mailvotech.point.trigger.thead.points',
                        'class': 'col-pointtrigger-points',
                }) }}

                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'point.trigger',
                        'orderBy': 't.id',
                        'text': 'mailvotech.core.id',
                        'class': 'col-pointtrigger-id visible-md visible-lg',
                }) }}
            </tr>
            </thead>
            <tbody>
            {% for item in items %}
                <tr>
                    <td>
                        {{ include('@MailVotechCore/Helper/list_actions.html.twig', {
                                'item': item,
                                'templateButtons': {
                                    'edit': permissions['point:triggers:edit'],
                                    'clone': permissions['point:triggers:create'],
                                    'delete': permissions['point:triggers:delete'],
                                },
                                'routeBase': 'pointtrigger',
                                'langVar': 'point.trigger',
                        }) }}
                    </td>
                    <td>
                        <div>
                            {{ include('@MailVotechCore/Helper/publishstatus_icon.html.twig', {'item': item, 'model': 'point.trigger'}) }}
                            {% if permissions['point:triggers:edit'] %}
                                <a href="{{ path('mailvotech_pointtrigger_action', {'objectAction': 'edit', 'objectId': item.id}) }}" data-toggle="ajax">
                                    {{ item.name }}
                                </a>
                            {% else %}
                                {{ item.name }}
                            {% endif %}
                            {{ customContent('trigger.name', _context) }}
                        </div>
                        {{ include('@MailVotechCore/Helper/description--inline.html.twig', {
                            'description': item.description
                        }) }}
                    </td>
                    <td class="visible-md visible-lg">
                        {{ include('@MailVotechCore/Modules/category--expanded.html.twig', {'category': item.category}) }}
                    </td>
                    <td class="visible-md visible-lg">
                        {{ item.group.name|default('mailvotech.point.group.form.nogroup'|trans) }}
                    </td>
                    <td>{{ item.points }}</td>
                    <td class="visible-md visible-lg">{{ item.id }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    <div class="panel-footer">
        {{ include('@MailVotechCore/Helper/pagination.html.twig', {
                'totalItems': items|length,
                'page': page,
                'limit': limit,
                'menuLinkId': 'mailvotech_pointtrigger_index',
                'baseUrl': path('mailvotech_pointtrigger_index'),
                'sessionVar': 'point.trigger',
        }) }}
    </div>
{% else %}
    {% if searchValue is not empty %}
        {{ include('@MailVotechCore/Helper/noresults.html.twig', {'tip': 'mailvotech.point.trigger.noresults.tip'}) }}
    {% else %}
        <div class="mt-80 col-md-offset-2 col-lg-offset-3 col-md-8 col-lg-5 height-auto">
            {% set childContainer %}
            <div class="mb-md">
                {% include '@MailVotechCore/Components/pictogram.html.twig' with {
                    'pictogram': 'react-to-data',
                    'size': '80'
                } %}
            </div>

            {{ include('@MailVotechCore/Components/content-item-row.html.twig', {
                type: 'default',
                eyebrow: 'mailvotech.point.trigger.onboarding.eyebrow',
                heading: 'mailvotech.point.trigger.onboarding.heading',
                copy: 'mailvotech.point.trigger.onboarding.copy',
            }) }}

            {% set triggersUseContainer %}
            <div class="mb-md">
                {% include '@MailVotechCore/Components/pictogram.html.twig' with {
                    'pictogram': 'construct',
                    'size': '64'
                } %}
            </div>
            <div class="type-body-02">
                {% include '@MailVotechCore/Components/list--styled.html.twig' with {
                    'type': 'ordered',
                    'style': 'numbers', 
                    'items': [
                        'mailvotech.point.trigger.onboarding.step1',
                        'mailvotech.point.trigger.onboarding.step2',
                        'mailvotech.point.trigger.onboarding.step3',
                    ]
                } %}
            </div>
            <div class="mt-lg">
                {{ include('@MailVotechCore/Notification/inline_notification.html.twig', {
                    'title': 'mailvotech.point.trigger.onboarding.notification.title',
                    'content': 'mailvotech.point.trigger.onboarding.notification.content',
                    'alert_type': 'success',
                    'dismissible': false,
                }) }}
            </div>
            {% endset %}

        {{ include('@MailVotechCore/Components/content-group.html.twig', {
            heading: 'mailvotech.point.trigger.onboarding.group.heading',
            childContainer: triggersUseContainer,
            cta: {
                'label': 'mailvotech.point.trigger.onboarding.cta',
                'link': path('mailvotech_pointtrigger_action', {'objectAction': 'new'}),
                attributes: {
                    'data-toggle': 'ajax',
                },
            }
        }) }}
        {% endset %}

        {{ include('@MailVotechCore/Components/content-block.html.twig', {
            heading: 'mailvotech.point.trigger.empty.heading',
            subheading: 'mailvotech.point.trigger.empty.subheading',
            childContainer: childContainer,
        }) }}
    </div>
    {% endif %}
{% endif %}
