{#
  Variables
    - activePage
    - items
#}
{% set isIndex = 'index' == tmpl ? true : false %}
{% set tmpl = 'list' %}
{% extends isIndex ? '@MailVotechCore/Default/content.html.twig' : '@MailVotechCore/Default/raw_output.html.twig' %}

{% block mailvotechContent %}pageresult{% endblock %}

{% block headerTitle %}
  {{ 'mailvotech.page.result.header.index'|trans({
        '%name%': activePage.getName(),
  }) }}
{% endblock %}

{% block actions %}
  {% set buttons = [
          {
              'attr': {
                  'target': '_new',
                  'data-toggle': '',
                  'class': 'btn btn-ghost btn-nospin',
                  'href': path('mailvotech_page_export', {'objectId': activePage.id, 'format': 'html'}),
              },
              'btnText': 'mailvotech.form.result.export.html'|trans,
              'iconClass': 'ri-file-code-line',
              'primary': true,
          },
          {
              'attr': {
                  'data-toggle': '',
                  'class': 'btn btn-ghost btn-nospin',
                  'href': path('mailvotech_page_export', {'objectId': activePage.id, 'format': 'csv'}),
              },
              'btnText': 'mailvotech.form.result.export.csv'|trans,
              'iconClass': 'ri-file-text-line',
              'primary': true,
          }
  ] %}

  {% if '\\PhpOffice\\PhpSpreadsheet\\Spreadsheet' is class %}
    {% set buttons = buttons|merge([{
          'attr': {
              'data-toggle': '',
              'class': 'btn btn-ghost btn-nospin',
              'href': path('mailvotech_page_export', {'objectId': activePage.id, 'format': 'xlsx'}),
          },
          'btnText': 'mailvotech.form.result.export.xlsx'|trans,
          'iconClass': 'ri-file-excel-2-fill',
          'primary': true,
    }]) %}
  {% endif %}

  {% set buttons = buttons|merge([{
        'attr': {
            'class': 'btn btn-ghost',
            'href': path('mailvotech_page_action', {'objectAction': 'view', 'objectId': activePage.id}),
            'data-toggle': 'ajax',
        },
        'iconClass': 'ri-close-line',
        'btnText': 'mailvotech.core.form.close'|trans,
  }]) %}

  {{ include('@MailVotechCore/Helper/page_actions.html.twig', {'customButtons': buttons}) }}
{% endblock %}

{% block content %}
  {% if isIndex %}
    <div class="page-list">
  {% endif %}

  {{ include('@MailVotechPage/Result/_list.html.twig') }}

  {% if isIndex %}
    </div>
  {% endif %}
{% endblock %}
