{% extends app.request.xmlHttpRequest ? '@MailVotechCore/Default/content.html.twig' : '@MailVotechUser/Security/base.html.twig' %}
{% block header %}{% trans %}mailvotech.user.auth.header{% endtrans %}{% endblock %}
{% block headerTitle %}{% trans %}mailvotech.user.auth.expired.header{% endtrans %}{% endblock %}

{% block content %}
    {% if app.request.xmlHttpRequest %}
        <div class="row">
            <div class="col-xs-12 col-sm-8 col-md-6 inline-login">
                {{ block('mainContent') }}
            </div>
        </div>
    {% else %}
        {{ block('mainContent') }}
    {% endif %}
{% endblock %}

{% block mainContent %}
	<form class="form-group login-form" name="login" data-toggle="ajax" role="form" action="{{ path('mailvotech_user_logincheck') }}" method="post">
		<div class="input-group mb-md">

			<span class="input-group-addon">
				<i class="ri-user-6-fill"></i>
			</span>
			<label for="username" class="sr-only">{% trans %}mailvotech.user.auth.form.loginusername{% endtrans %}</label>
			<input type="text" id="username" name="_username" class="form-control input-lg" value="{{ last_username|escape }}" required autofocus placeholder="{% trans %}mailvotech.user.auth.form.loginusername{% endtrans %}"/>
		</div>
		<div class="input-group mb-md">
			<span class="input-group-addon">
				<i class="ri-key-2-line"></i>
			</span>
			<label for="password" class="sr-only">{% trans %}mailvotech.core.password{% endtrans %}:</label>
			<input type="password" id="password" name="_password" class="form-control input-lg" required placeholder="{% trans %}mailvotech.core.password{% endtrans %}"/>
		</div>

		<div class="checkbox-inline custom-primary pull-left mb-md">
			<label for="remember_me">
				<input type="checkbox" id="remember_me" name="_remember_me"/>
				<span></span>
				{% trans %}mailvotech.user.auth.form.rememberme{% endtrans %}
			</label>
		</div>

		<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate')|escape }}"/>
		{% include '@MailVotechCore/Helper/button.html.twig' with {
			buttons: [
				{
					label: 'mailvotech.user.auth.form.loginbtn',
					variant: 'primary',
					size: 'lg',
					wide: true
				}
			]
		} %}

		<div class="mt-sm text-right">
			<a href="{{ path('mailvotech_user_passwordreset') }}">{% trans %}mailvotech.user.user.passwordreset.link{% endtrans %}</a>
		</div>
	</form>
	{% if integrations is not empty %}
		<ul class="list-group">
			{% for sso in integrations %}
				<a href="{{ path('mailvotech_sso_login', {'integration' : sso.getName()}) }}" class="list-group-item">
					<img class="pull-left mr-xs" style="height: 16px;" src="{{ getAssetUrl(sso.getIcon()) }}">
					<p class="list-group-item-heading">{{ ('mailvotech.integration.sso.' ~ sso.getName())|trans }}</p>
				</a>
			{% endfor %}
		</ul>
	{% endif %}
{% endblock %}
