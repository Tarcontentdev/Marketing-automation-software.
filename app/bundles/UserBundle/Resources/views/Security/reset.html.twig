{% extends app.request.xmlHttpRequest ? '@MailVotechCore/Default/content.html.twig' : '@MailVotechUser/Security/base.html.twig' %}

{% block content %}
    <div class="alert alert-warning">{% trans %}mailvotech.user.user.passwordreset.info{% endtrans %}</div>
        {{ form_start(form) }}
        {{ form_row(form.identifier) }}
        {{ form_widget(form.submit) }}
        {{ form_end(form) }}
    <div class="mt-sm">
        <a href="{{ path('login') }}">{% trans %}mailvotech.user.user.passwordreset.back{% endtrans %}</a>
    </div>
{% endblock %}
