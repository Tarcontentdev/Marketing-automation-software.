{% set isIndex = tmpl == 'index' ? true : false %}
{% set tmpl = 'list' %}

{% extends isIndex ? '@MailVotechCore/Default/content.html.twig' : '@MailVotechCore/Default/raw_output.html.twig' %}
{% block mailvotechContent %}asset
{% endblock %}
{% block headerTitle %}
	{% trans %}mailvotech.asset.assets{% endtrans %}
{% endblock %}

{% block content %}
	{% if isIndex %}
		<div id="page-list-wrapper" class="{% if items|length > 0 or searchValue is not empty %}panel {% endif %}panel-default">

			{{- include('@MailVotechCore/Helper/list_toolbar.html.twig', {
                'searchValue': searchValue,
                'action': currentRoute,
                'page_actions': {
                    'templateButtons': {
                        'new': permissions['asset:assets:create'],
                    },
                    'routeBase': 'asset',
                    'langVar': 'asset.asset',
                },
                'bulk_actions': {
                    'langVar': 'asset.asset',
                    'routeBase': 'asset',
                    'templateButtons': {
                        'delete': permissions['asset:assets:deleteown'] or permissions['asset:assets:deleteother'],
                    },
                },
                'quickFilters': [
                    {
                        'search': 'mailvotech.core.searchcommand.isuncategorized',
                        'label': 'mailvotech.core.form.uncategorized',
                        'tooltip': 'mailvotech.core.search.quickfilter.is_uncategorized',
                        'icon': 'ri-folder-unknow-line'
                    },
                    {
                        'search': 'mailvotech.core.searchcommand.ispublished',
                        'label': 'mailvotech.core.form.available',
                        'tooltip': 'mailvotech.core.search.quickfilter.is_published',
                        'icon': 'ri-check-line'
                    },
                    {
                        'search': 'mailvotech.core.searchcommand.isunpublished',
                        'label': 'mailvotech.core.form.unavailable',
                        'tooltip': 'mailvotech.core.search.quickfilter.is_unpublished',
                        'icon': 'ri-close-line'
                    },
                    {
                        'search': 'mailvotech.core.searchcommand.ismine',
                        'label': 'mailvotech.core.searchcommand.ismine.label',
                        'tooltip': 'mailvotech.core.searchcommand.ismine.description',
                        'icon': 'ri-user-line'
                    },
                    {
                        'search': 'mailvotech.asset.asset.searchcommand.isexpired',
                        'label': 'mailvotech.core.form.no_longer_available',
                        'tooltip': 'mailvotech.asset.asset.searchcommand.isexpired.description',
                        'icon': 'ri-time-line'
                    },
                    {
                        'search': 'mailvotech.asset.asset.searchcommand.ispending',
                        'label': 'mailvotech.core.form.not_yet_available',
                        'tooltip': 'mailvotech.asset.asset.searchcommand.ispending.description',
                        'icon': 'ri-timer-line'
                    }
                ]
            }) -}}
			<div class="page-list">
				{{ block('listResults') }}
			</div>
		</div>
	{% else %}
		{{ block('listResults') }}
	{% endif %}
{% endblock %}

{% block listResults %}
{% if items|length %}
    <div class="table-responsive">
        <table class="table table-hover asset-list" id="assetTable">
            <thead>
            <tr>
                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                    'checkall': 'true',
                    'target': '#assetTable',
                }) }}
                {{- include(
                    '@MailVotechCore/Helper/tableheader.html.twig',
                    {
                        'sessionVar' : 'asset',
                        'orderBy'    : 'a.title',
                        'text'       : 'mailvotech.core.title',
                        'class'      : 'col-asset-title',
                    }
                ) -}}
                {{- include(
                    '@MailVotechCore/Helper/tableheader.html.twig',
                    {
                        'sessionVar' : 'asset',
                        'orderBy'    : 'c.title',
                        'text'       : 'mailvotech.core.category',
                        'class'      : 'visible-md visible-lg col-asset-category',
                    }
                ) -}}
                {{- include(
                    '@MailVotechCore/Helper/tableheader.html.twig',
                    {
                        'sessionVar' : 'asset',
                        'orderBy'    : 'a.downloadCount',
                        'text'       : 'mailvotech.asset.asset.thead.download.count',
                        'class'      : 'visible-md visible-lg col-asset-download-count',
                    }
                ) -}}
                {{- include(
                    '@MailVotechCore/Helper/tableheader.html.twig',
                    {
                        'sessionVar' : 'asset',
                        'orderBy'    : 'a.dateAdded',
                        'text'       : 'mailvotech.lead.import.label.dateAdded',
                        'class'      : 'visible-md visible-lg col-asset-dateAdded',
                    }
                ) -}}
                {{- include(
                    '@MailVotechCore/Helper/tableheader.html.twig',
                    {
                        'sessionVar' : 'asset',
                        'orderBy'    : 'a.dateModified',
                        'text'       : 'mailvotech.lead.import.label.dateModified',
                        'class'      : 'visible-md visible-lg col-asset-dateModified',
                        'default'    : true,
                    }
                ) -}}
                {{- include(
                    '@MailVotechCore/Helper/tableheader.html.twig',
                    {
                        'sessionVar' : 'asset',
                        'orderBy'    : 'a.createdByUser',
                        'text'       : 'mailvotech.core.createdby',
                        'class'      : 'visible-md visible-lg col-asset-createdByUser',
                    }
                ) -}}
                {{- include(
                    '@MailVotechCore/Helper/tableheader.html.twig',
                    {
                        'sessionVar' : 'asset',
                        'orderBy'    : 'a.id',
                        'text'       : 'mailvotech.core.id',
                        'class'      : 'visible-md visible-lg col-asset-id',
                    }
                ) -}}
            </tr>
            </thead>
            <tbody>
            {% for k, item in items %}
                <tr>
                    <td>
                        {{- include(
                            '@MailVotechCore/Helper/list_actions.html.twig',
                            {
                                'item'            : item,
                                'templateButtons' : {
                                    'edit' : securityHasEntityAccess(
                                        permissions['asset:assets:editown'],
                                        permissions['asset:assets:editother'],
                                        item.getCreatedBy()
                                    ),
                                    'delete' : securityHasEntityAccess(
                                        permissions['asset:assets:deleteown'],
                                        permissions['asset:assets:deleteother'],
                                        item.getCreatedBy()
                                    ),
                                    'clone' : permissions['asset:assets:create'],
                                },
                                'routeBase'     : 'asset',
                                'langVar'       : 'asset.asset',
                                'nameGetter'    : 'getTitle',
                                'customButtons' : {
                                    0: {
                                        'attr' : {
                                            'data-toggle' : 'ajaxmodal',
                                            'data-target' : '#AssetPreviewModal',
                                            'href'        : path(
                                                'mailvotech_asset_action',
                                                {'objectAction' : 'preview', 'objectId' : item.getId(), 'stream': 0}
                                            ),
                                        },
                                        'btnText'   : 'mailvotech.asset.asset.preview'|trans,
                                        'iconClass' : 'ri-image-circle-line',
                                    },
                                    1: {
                                        'attr' : {
                                            'data-copy' : url('mailvotech_asset_download', {'slug': item.getSlug()}),
                                            'data-toggle' : 'none',
                                        },
                                        'btnText'   : 'mailvotech.core.copy_download_link'|trans,
                                        'iconClass' : 'ri-clipboard-line',
                                    },
                                },
                            }
                        ) -}}
                    </td>
                    <td>
                        <div>
                            {{- include(
                                '@MailVotechCore/Helper/publishstatus_icon.html.twig',
                                {
                                    'item'  : item,
                                    'model' : 'asset.asset',
                                }
                            ) -}}
                            <a href="{{ path(
                                'mailvotech_asset_action',
                                {'objectAction' : 'view', 'objectId' : item.getId()}
                            ) }}"
                               data-toggle="ajax">
                                {{ item.getTitle() }}
                            </a>
                            <i class="{{ item.getIconClass() }}"></i>
                            {{ customContent('asset.name', _context) }}
                            {{ include('@MailVotechProject/Modules/projects.html.twig') }}
                        </div>
                        {% set description = item.getDescription() %}
                        {% if description %}
                            {{ include('@MailVotechCore/Helper/description--inline.html.twig', {
                                'description': description
                            }) }}
                        {% endif %}
                    </td>
                    <td class="visible-md visible-lg">
                        {{ include('@MailVotechCore/Modules/category--expanded.html.twig', {'category': item.getCategory()}) }}
                    </td>
                    <td class="visible-md visible-lg">{{ item.getDownloadCount() }}</td>
                    <td class="visible-md visible-lg" title="{{ item.getDateAdded() ? dateToFullConcat(item.getDateAdded()) : '' }}">
                        {{ item.getDateAdded() ? dateToDate(item.getDateAdded()) : '' }}
                    </td>
                    <td class="visible-md visible-lg" title="{{ item.getDateModified() ? dateToFullConcat(item.getDateModified()) : '' }}">
                        {{ item.getDateModified() ? dateToDate(item.getDateModified()) : '' }}
                    </td>
                    <td class="visible-md visible-lg">{{ item.getCreatedByUser() }}</td>
                    <td class="visible-md visible-lg">{{ item.getId() }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

    <div class="panel-footer">
        {{- include('@MailVotechCore/Helper/pagination.html.twig', {
                'totalItems' : items|length,
                'page'       : page,
                'limit'      : limit,
                'menuLinkId' : 'mailvotech_asset_index',
                'baseUrl'    : path('mailvotech_asset_index'),
                'sessionVar' : 'asset',
        }) -}}
    </div>
{% else %}
    {% if searchValue is not empty %}
        {{- include('@MailVotechCore/Helper/noresults.html.twig', {'tip' : 'mailvotech.asset.noresults.tip'}) -}}
    {% else %}
        <div class="mt-80 col-md-offset-2 col-lg-offset-3 col-md-8 col-lg-5 height-auto">
            {% set childContainer %}
                <div class="mb-md">
                    {% include '@MailVotechCore/Components/pictogram.html.twig' with {
                        'pictogram': 'cloud--assets',
                        'size': '80'
                    } %}
                </div>
            {% endset %}
    
            {{ include('@MailVotechCore/Components/content-block.html.twig', {
                heading: 'mailvotech.asset.onboarding.heading',
                subheading: 'mailvotech.asset.onboarding.subheading',
                copy: 'mailvotech.asset.onboarding.copy',
                childContainer: childContainer,
            }) }}
        </div>
    {% endif %}
{% endif %}

{{- include('@MailVotechCore/Helper/modal.html.twig', {
        'id'     : 'AssetPreviewModal',
        'header' : false,
}) -}}

{{ include('@MailVotechCore/Modules/protip.html.twig', {
    tip: random(['mailvotech.protip.assets.gating', 'mailvotech.protip.assets.naming', 'mailvotech.protip.assets.repurpose', 'mailvotech.protip.assets.track'])
}) }}
{% endblock %}