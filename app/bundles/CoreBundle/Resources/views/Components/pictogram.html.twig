{% set svg_path = '@MailVotechCore/Assets/pictograms/' ~ pictogram ~ '.svg' %}

<div class="pictogram animation--slide-in-up pictogram--size-{{ size }}" style="fill: {{ color|default('var(--icon-primary)') }}">
    {{ source(svg_path) }}
</div>
