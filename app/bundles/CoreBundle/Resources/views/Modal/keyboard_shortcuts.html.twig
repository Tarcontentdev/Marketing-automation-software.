<div class="modal fade" id="keyboardShortcutsModal" tabindex="-1" role="dialog"
  aria-labelledby="keyboardShortcutsModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-sm" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="{{ 'mailvotech.core.close' | trans }}">
          <span aria-hidden="true">&times;</span>
        </button>
        <h4 class="modal-title" id="keyboardShortcutsModalLabel">
          {{ "mailvotech.keyboard.shortcuts" | trans }}
        </h4>
      </div>
      <div class="modal-body modal-scroll-content">
        <div class="row-no-gutters">
          <div class="col-md-12">
            {% set shortcuts = {
            'navigation': {
            'g+d': 'mailvotech.keyboard.load_dashboard',
            'g+c': 'mailvotech.keyboard.load_contacts',
            'g+e': 'mailvotech.keyboard.load_emails',
            'g+f': 'mailvotech.keyboard.load_forms',
            'g+s': 'mailvotech.keyboard.load_segments',
            'g+p': 'mailvotech.keyboard.load_landing_pages',
            },
            'functions': {
            '/' : 'mailvotech.keyboard.list_search',
            'f+/': 'mailvotech.keyboard.global_search',
            'f+n': 'mailvotech.keyboard.show_notifications',
            'f+m': 'mailvotech.keyboard.open_settings',
            },
            'actions': {
            'e' : 'mailvotech.keyboard.edit',
            'c' : 'mailvotech.keyboard.create',
            '⌘+Enter' : 'mailvotech.keyboard.save',
            'g+i' : 'mailvotech.keyboard.back',
            'Del / ⌘+⌫' : 'mailvotech.keyboard.delete',
            } } %}
            <h4 class="fw-sb">
              {{ "mailvotech.keyboard.section.global" | trans }}
            </h4>
            {% for section, shortcut_list in shortcuts %}
            <h5 class="mb-xs mt-md fw-sb">
              {{ ('mailvotech.keyboard.section.' ~ section)|trans }}
            </h5>
            <table class="shortcut-table bsv-xs">
              {% for key, description in shortcut_list %}
              <tr class="mb-xs">
                <td class="shortcut-description">
                  {{ description | trans }}
                </td>
                <td class="shortcut-keys text-right">
                  {% set parts = key|split(' / ') %}
                  {% for part in parts %}
                    {% if not loop.first %}
                      / <!-- Add the separator outside of the <kbd> tags -->
                    {% endif %}
                    {% set keys = part|split('+') %}
                    {% for k in keys %}
                      {% set title = '' %}
                      {% if k == '⌫' %}
                        {% set title = 'mailvotech.keyboard.backspace'|trans %}
                      {% elseif k == '⌘' %}
                        {% set title = 'mailvotech.keyboard.command'|trans %}
                     {% endif %}
                  <kbd{% if title %} title="{{ title }}" data-toggle="tooltip" {% endif %}>{{ k }}</kbd>
                    {% endfor %}
                  {% endfor %}
                </td>
              </tr>
              {% endfor %}
            </table>
            {% endfor %}
          </div>
        </div>
        <div class="row">
          <hr />
          <ul class="accordion" id="accordion" role="tablist" aria-multiselectable="true">
            {{ include('@MailVotechLead/KeyboardShortcuts/lead_shortcuts.html.twig', ignore_missing = true) }}
            {{ include('@MailVotechEmail/KeyboardShortcuts/emails_shortcuts.html.twig', ignore_missing = true) }}
          </ul>
          <div class="alert alert-info col-xs-12">
            <div class="d-flex jc-space-between ai-center small">
              <div> <strong class="mr-xs">{{ 'mailvotech.keyboard.tip'|trans }}</strong><kbd>⌘</kbd> <i
                  class="ri-arrow-right-line"></i> <kbd>Ctrl</kbd></div>
            </div>
          </div>
        </div>
      </div>
      <div class="modal-footer">
        <p>
          {{ 'mailvotech.keyboard.press_to_view'|trans({'%shift%': '', '%question%': '<kbd>?</kbd>'})|raw }}
        </p>
      </div>
    </div>
  </div>
</div>
