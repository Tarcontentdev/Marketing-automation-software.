<div class="bundle-form">
    <div class="bundle-form-header mb-15">
        <h3>{{ 'mailvotech.campaign.leadsource.header.singular'|trans }}</h3>
        <h6 class="text-secondary">{{ ('mailvotech.campaign.leadsource.'~sourceType~'.tooltip')|trans }}</h6>
    </div>
    {{ form_start(form) }}
        <input type="hidden" name="submit" value="1">
    {{ form_end(form) }}
</div>
