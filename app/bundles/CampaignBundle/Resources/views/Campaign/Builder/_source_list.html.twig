{#
  Variables
    - campaignSources
    - campaignId
#}
<div id="SourceGroupList" class="hide">
    <h4 class="mb-xs">
        <span>{{ 'mailvotech.campaign.leadsource.header'|trans }}</span>
    </h4>
    <select id="SourceList" class="campaign-event-selector">
        <option value=""></option>
        {% for option in ['lists', 'forms'] %}
            <option id="campaignLeadSource_{{ option }}"
                    class="option_campaignLeadSource_{{ option }}"
                    data-href="{{ path('mailvotech_campaignsource_action', {'objectAction': 'new', 'objectId': campaignId, 'sourceType': option}) }}"
                    data-target="#CampaignEventModal"
                    title="{{ ('mailvotech.campaign.leadsource.'~option~'.tooltip')|trans|e }}"
                    value="{{ option|e }}"
                    {% if campaignSources[option] is defined %}disabled{% endif %}>
                <span>{{ ('mailvotech.campaign.leadsource.'~option)|trans }}</span>
            </option>
        {% endfor %}
    </select>
</div>
