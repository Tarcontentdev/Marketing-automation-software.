{% set isIndex = tmpl == 'index' ? true : false %}
{% set tmpl = 'list' %}

{% extends isIndex ? '@MailVotechCore/Default/content.html.twig' : '@MailVotechCore/Default/raw_output.html.twig' %}

{% block headerTitle %}{{ 'mailvotech.campaign.campaigns'|trans }}{% endblock %}


{% block content %}
  {% if isIndex %}
    <div id="page-list-wrapper" class="{% if items|length > 0 or searchValue is not empty %}panel {% endif %}panel-default">
        {%- set pageButtons = [] %}
        {% if permissions['campaign:campaigns:create'] %}
            {% if permissions['campaign:imports:create'] %}
                {% set pageButtons = pageButtons|merge([
                    {
                        'attr': {
                            'href': path('mailvotech_campaign_import_action', {'objectAction': 'new'}),
                            'data-toggle': '0',
                        },
                        'iconClass': 'ri-import-line',
                        'btnText': 'mailvotech.campaign.campaign.import',
                    }
                ]) %}
            {% endif %}
        {% endif %}

        {{- include('@MailVotechCore/Helper/list_toolbar.html.twig', {
            'searchValue': searchValue,
            'action': currentRoute,
            'actionRoute': actionRoute,
            'indexRoute': indexRoute,
            'translationBase': translationBase,
            'preCustomButtons': toolBarButtons|default(null),
            'templateButtons': {
                'delete': permissions[permissionBase~':delete'],
            },
            'filters': filters|default([]),
            'page_actions': {
                'templateButtons': {
                    'new': permissions['campaign:campaigns:create'],
                },
                'routeBase': 'campaign',
                'langVar': 'campaign.campaigns',
                'customButtons': pageButtons,
            },
            'bulk_actions': {
                'routeBase': 'campaign',
                'templateButtons': {
                    'delete': permissions['campaign:campaigns:deleteown'] or permissions['campaign:campaigns:deleteother'],
                },
            },
            'quickFilters': [
                {
                    'search': 'mailvotech.core.searchcommand.ispublished',
                    'label': 'mailvotech.core.form.active',
                    'tooltip': 'mailvotech.core.searchcommand.ispublished.description',
                    'icon': 'ri-check-line'
                },
                {
                    'search': 'mailvotech.core.searchcommand.isunpublished',
                    'label': 'mailvotech.core.form.inactive',
                    'tooltip': 'mailvotech.core.searchcommand.isunpublished.description',
                    'icon': 'ri-close-line'
                },
                {
                    'search': 'mailvotech.core.searchcommand.isuncategorized',
                    'label': 'mailvotech.core.form.uncategorized',
                    'tooltip': 'mailvotech.core.searchcommand.isuncategorized.description',
                    'icon': 'ri-folder-unknow-line'
                },
                {
                    'search': 'mailvotech.core.searchcommand.ismine',
                    'label': 'mailvotech.core.searchcommand.ismine.label',
                    'tooltip': 'mailvotech.core.searchcommand.ismine.description',
                    'icon': 'ri-user-line'
                },
                {
                    'search': 'mailvotech.campaign.campaign.searchcommand.isexpired',
                    'label': 'mailvotech.core.form.no_longer_available',
                    'tooltip': 'mailvotech.campaign.campaign.searchcommand.isexpired.description',
                    'icon': 'ri-time-line'
                },
                {
                    'search': 'mailvotech.campaign.campaign.searchcommand.ispending',
                    'label': 'mailvotech.core.form.not_yet_available',
                    'tooltip': 'mailvotech.campaign.campaign.searchcommand.ispending.description',
                    'icon': 'ri-timer-line'
                }
            ]
        }) -}}
        <div class="page-list">
            {{- customContent('content.above', _context) -}}
  {% endif %}

  {% if items|length > 0 %}
    <div class="table-responsive">
        <table class="table table-hover campaign-list" id="campaignTable">
          <thead>
            <tr>
                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                    'checkall': 'true',
                    'target': '#campaignTable',
                }) }}

              {{- include('@MailVotechCore/Helper/tableheader.html.twig', {
                  'sessionVar': 'campaign',
                  'orderBy': 'c.name',
                  'text': 'mailvotech.core.name',
                  'class': 'col-campaign-name',
              }) -}}

              {{- include('@MailVotechCore/Helper/tableheader.html.twig', {
                  'sessionVar': 'campaign',
                  'orderBy': 'cat.title',
                  'text': 'mailvotech.core.category',
                  'class': 'visible-md visible-lg col-campaign-category',
              }) -}}

              {{- include('@MailVotechCore/Helper/tableheader.html.twig', {
                  'sessionVar': 'campaign',
                  'orderBy': 'c.dateAdded',
                  'text': 'mailvotech.lead.import.label.dateAdded',
                  'class': 'visible-md visible-lg col-campaign-dateAdded',
              }) -}}

              {{- include('@MailVotechCore/Helper/tableheader.html.twig', {
                  'sessionVar': 'campaign',
                  'orderBy': 'c.dateModified',
                  'text': 'mailvotech.lead.import.label.dateModified',
                  'class': 'visible-md visible-lg col-campaign-dateModified',
                  'default': true,
              }) -}}

              {{- include('@MailVotechCore/Helper/tableheader.html.twig', {
                  'sessionVar': 'campaign',
                  'orderBy': 'c.createdByUser',
                  'text': 'mailvotech.core.createdby',
                  'class': 'visible-md visible-lg col-campaign-createdByUser',
              }) -}}

              {{- include('@MailVotechCore/Helper/tableheader.html.twig', {
                  'sessionVar': 'campaign',
                  'orderBy': 'c.id',
                  'text': 'mailvotech.core.id',
                  'class': 'visible-md visible-lg col-campaign-id',
              }) -}}
            </tr>
          </thead>
          <tbody>
            {% for item in items %}
                <tr>
                    <td>
                      {{- include('@MailVotechCore/Helper/list_actions.html.twig', {
                          'item': item,
                          'templateButtons' : {
                          'edit': securityHasEntityAccess(
                                permissions['campaign:campaigns:editown'],
                                permissions['campaign:campaigns:editother'],
                                item.createdBy
                              ),
                          'clone': permissions['campaign:campaigns:create'],
                          'export': enableExportPermission,
                          'delete': securityHasEntityAccess(
                                permissions['campaign:campaigns:deleteown'],
                                permissions['campaign:campaigns:deleteother'],
                                item.createdBy
                              ),
                          },
                          'routeBase': 'campaign',
                      }) -}}
                    </td>
                    <td>
                        <div>
                          {{ include('@MailVotechCore/Helper/publishstatus_icon.html.twig', {
                              'item': item,
                              'model': 'campaign',
                              'onclick': item.onclickMethod,
                              'attributes': item.dataAttributes,
                              'transKeys': item.translationKeysDataAttributes,
                          }) }}
                            <a href="{{ path('mailvotech_campaign_action', {'objectAction': 'view', 'objectId': item.id}) }}" data-toggle="ajax">
                                {{- item.name -}}
                                {{- customContent('campaign.name', _context|merge({'item': item})) -}}
                            </a>
                            {{ include('@MailVotechProject/Modules/projects.html.twig') }}
                        </div>

                        {{ include('@MailVotechCore/Helper/description--inline.html.twig', {
                            'description': item.description
                        }) }}

                    </td>
                    <td class="visible-md visible-lg">
                        {{ include('@MailVotechCore/Modules/category--expanded.html.twig', {'category': item.category}) }}
                    </td>
                    <td class="visible-md visible-lg" title="{{ item.dateAdded ? dateToFullConcat(item.dateAdded) : '' }}">
                        {{ item.dateAdded ? dateToDate(item.dateAdded) : '' }}
                    </td>
                    <td class="visible-md visible-lg" title="{{ item.dateModified ? dateToFullConcat(item.dateModified) : '' }}">
                        {{ item.dateModified ? dateToDate(item.dateModified) : '' }}
                    </td>
                    <td class="visible-md visible-lg">{{ item.createdByUser }}</td>
                    <td class="visible-md visible-lg">{{ item.id }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    <div class="panel-footer">
        {{- include('@MailVotechCore/Helper/pagination.html.twig', {
            'totalItems': items|length,
            'page': page,
            'limit': limit,
            'menuLinkId': 'mailvotech_campaign_index',
            'baseUrl': path('mailvotech_campaign_index'),
            'sessionVar': 'campaign',
        }) -}}
    </div>
  {% else %}
    {% if searchValue is not empty %}
        {{- include('@MailVotechCore/Helper/noresults.html.twig', {'tip' : 'mailvotech.campaign.noresults.tip'}) -}}
    {% else %}
        <div class="mt-80 col-md-offset-2 col-lg-offset-3 col-md-8 col-lg-5 height-auto">
            {% set childContainer %}
                <div class="mt-32 mb-md">
                    {% include '@MailVotechCore/Components/pictogram.html.twig' with {
                        'pictogram': 'ibm--automation-platform',
                        'size': '80'
                    } %}
                </div>
            {% endset %}

            {{ include('@MailVotechCore/Components/content-block.html.twig', {
                heading: 'mailvotech.campaign.contentblock.heading',
                subheading: 'mailvotech.campaign.contentblock.subheading',
                copy: 'mailvotech.campaign.contentblock.copy',
                childContainer: childContainer,
            }) }}
        </div>
    {% endif %}
  {% endif %}

  {% if isIndex %}
            {{- customContent('content.below', _context) -}}
        </div>
    </div>

    {{ include('@MailVotechCore/Modules/protip.html.twig', {
        tip: random(['mailvotech.protip.campaigns.reengagement', 'mailvotech.protip.campaigns.survey', 'mailvotech.protip.campaigns.crosssell', 'mailvotech.protip.campaigns.onboarding'])
    }) }}
  {% endif %}
{% endblock %}
