{% block content %}
<table class="table table-hover">
    <thead>
        <tr>
            <th style="width: 50px;"></th>
            <th>{{ 'mailvotech.core.name'|trans }}</th>
        </tr>
    </thead>
    <tbody>
        {% for k in clients %}
        <tr>
            <td>
                {{- include('@MailVotechCore/Helper/confirm.html.twig', {
                    'btnClass'      : 'btn btn-danger btn-xs',
                    'message'       : 'mailvotech.api.client.form.confirmrevoke'|trans({'%name%' : k.getName()}),
                    'confirmText'   : 'mailvotech.api.client.form.revoke'|trans,
                    'confirmAction' : path('mailvotech_client_action', {'objectAction' : 'revoke', 'objectId' : k.getId()}),
                    'iconClass'     : 'ri-delete-bin-line',
                    'btnText'       : 'mailvotech.api.client.form.revoke'|trans,
                }) -}}
            </td>
            <td>{{ k.getName() }}</td>
        </tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}
