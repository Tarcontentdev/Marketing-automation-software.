{% extends '@MailVotechUser/Security/base.html.twig' %}
{% block headerTitle %}{{ 'mailvotech.api.oauth.header'|trans }}{% endblock %}
{% block content %}
<form class="form-group login-form" name="login" data-toggle="ajax" role="form" action="{{ path(route) }}" method="post">
    <div class="input-group mb-md">
        <span class="input-group-addon"><i class="ri-user-6-fill"></i></span>
        <label for="username" class="sr-only">{{ 'mailvotech.user.auth.form.loginusername'|trans }}</label>
        <input type="text" id="username" name="_username" class="form-control input-lg" value="{{ last_username }}" required autofocus placeholder="{{ 'mailvotech.user.auth.form.loginusername'|trans }}" />
    </div>
    <div class="input-group mb-md">
        <span class="input-group-addon"><i class="ri-key-2-line"></i></span>
        <label for="password" class="sr-only">{{ 'mailvotech.core.password'|trans }}:</label>
        <input type="password" id="password" name="_password" class="form-control input-lg" required placeholder="{{ 'mailvotech.core.password'|trans }}" />
    </div>
    <button class="btn btn-lg btn-primary btn-block" type="submit">{{ 'mailvotech.user.auth.form.loginbtn'|trans }}</button>
</form>
{% endblock %}
