{#
  Variables
    - items
    - leadCounts
    - page
    - limit
#}
{# Check to see if the entire page should be displayed or just main content #}
{% set isIndex = 'index' == tmpl ? true : false %}
{% set tmpl = 'list' %}
{% extends isIndex ? '@MailVotechCore/Default/content.html.twig' : '@MailVotechCore/Default/raw_output.html.twig' %}

{% block mailvotechContent %}leadlist{% endblock %}

{% block headerTitle %}{{ 'mailvotech.lead.list.header.index'|trans }}{% endblock %}

{% block content %}
  {% set listCommand = 'mailvotech.lead.lead.searchcommand.list'|trans %}

  {% if isIndex %}
    <div id="page-list-wrapper" class="{% if items|length > 0 or searchValue is not empty %}panel {% endif %}panel-default">
      {{- include('@MailVotechCore/Helper/list_toolbar.html.twig', {
        'searchValue': searchValue,
        'action': currentRoute,
        'filters': filters|default([]),
        'page_actions': {
            'templateButtons': {'new': permissions[constant('MailVotech\\LeadBundle\\Security\\Permissions\\LeadPermissions::LISTS_CREATE')]},
            'routeBase': 'segment',
            'langVar': 'lead.list',
            'tooltip': 'mailvotech.lead.lead.segment.add.help',
        },
        'bulk_actions': {
          'langVar': 'lead.list',
          'routeBase': 'segment',
          'templateButtons': {
              'delete': permissions['lead:lists:deleteother'],
          },
        },
        'quickFilters': [
            {
                'search': 'mailvotech.core.searchcommand.ismine',
                'label': 'mailvotech.core.searchcommand.ismine.label',
                'tooltip': 'mailvotech.core.searchcommand.ismine.description',
                'icon': 'ri-user-line'
            },
            {
                'search': 'mailvotech.core.searchcommand.ispublished',
                'label': 'mailvotech.core.form.active',
                'tooltip': 'mailvotech.core.search.quickfilter.is_published',
                'icon': 'ri-check-line'
            },
            {
                'search': 'mailvotech.core.searchcommand.isunpublished',
                'label': 'mailvotech.core.form.inactive',
                'tooltip': 'mailvotech.core.search.quickfilter.is_unpublished',
                'icon': 'ri-close-line'
            }
        ]
    }) -}}
    <div class="page-list">
  {% endif %}

  {{ include('@MailVotechLead/List/_list.html.twig') }}

  {% if isIndex %}
        </div>
    </div>
    {{ include('@MailVotechCore/Modules/protip.html.twig', {
      tip: random(['mailvotech.protip.segments.dynamic', 'mailvotech.protip.segments.test'])
  }) }}
  {% endif %}
{% endblock %}
