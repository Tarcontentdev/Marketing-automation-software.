{#
  Variables
    - items
    - leadCounts
    - page
    - limit
#}
{% if items|length > 0 %}
    <div class="table-responsive">
        <table class="table table-hover" id="leadListTable">
            <thead>
            <tr>
                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'checkall': 'true',
                        'target': '#leadListTable',
                    }) }}
                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'lead.list',
                        'orderBy': 'l.name',
                        'text': 'mailvotech.core.name',
                        'class': 'col-leadlist-name',
                }) }}
                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'lead.list',
                        'text': 'mailvotech.lead.list.thead.leadcount',
                        'class': 'visible-md visible-lg col-leadlist-leadcount',
                }) }}
                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'lead.list',
                        'orderBy': 'l.dateAdded',
                        'text': 'mailvotech.lead.import.label.dateAdded',
                        'class': 'visible-md visible-lg col-leadlist-dateAdded',
                }) }}
                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'lead.list',
                        'orderBy': 'l.dateModified',
                        'text': 'mailvotech.lead.import.label.dateModified',
                        'class': 'visible-md visible-lg col-leadlist-dateModified',
                        'default': true,
                }) }}
                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'lead.list',
                        'orderBy': 'l.createdByUser',
                        'text': 'mailvotech.core.createdby',
                        'class': 'visible-md visible-lg col-leadlist-createdByUser',
                }) }}
                {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'sessionVar': 'lead.list',
                        'orderBy': 'l.id',
                        'text': 'mailvotech.core.id',
                        'class': 'visible-md visible-lg col-leadlist-id',
                }) }}
            </tr>
            </thead>
            <tbody>
              {% for item in items %}
                {{ include('@MailVotechLead/List/_list_row.html.twig', {'item': item}) }}
              {% endfor %}
            </tbody>
        </table>
        <div class="panel-footer">
            {{ include('@MailVotechCore/Helper/pagination.html.twig', {
                    'totalItems': items|length,
                    'page': page,
                    'limit': limit,
                    'baseUrl': path('mailvotech_segment_index'),
                    'sessionVar': 'lead.list',
            }) }}
        </div>
    </div>
{% else %}
    {% if searchValue is not empty %}
        {{- include('@MailVotechCore/Helper/noresults.html.twig') -}}
    {% else %}
        <div class="mt-80 col-md-offset-2 col-lg-offset-3 col-md-8 col-lg-5 height-auto">
            {% set childContainer %}
                <div class="mt-32 mb-md">
                    {% include '@MailVotechCore/Components/pictogram.html.twig' with {
                        'pictogram': 'circle--packing',
                        'size': '80'
                    } %}
                </div>
            {% endset %}

            {{ include('@MailVotechCore/Components/content-block.html.twig', {
                heading: 'mailvotech.lead.list.contentblock.heading',
                subheading: 'mailvotech.lead.list.contentblock.subheading',
                copy: 'mailvotech.lead.list.contentblock.copy',
                childContainer: childContainer,
            }) }}
        </div>
    {% endif %}
{% endif %}
