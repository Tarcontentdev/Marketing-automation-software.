{#
  Variables
    - items
#}
{% if items|length > 0 %}
  <div class="table-responsive">
      <table class="table table-hover" id="importTable">
          <thead>
              <tr>
                  {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                      'sessionVar': sessionVar,
                      'orderBy': tablePrefix~'.status',
                      'text': 'mailvotech.lead.import.status',
                      'class': 'col-status',
                  }) }}
                  {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                      'sessionVar': sessionVar,
                      'orderBy': tablePrefix~'.originalFile',
                      'text': 'mailvotech.lead.import.source.file',
                      'class': 'col-original-file',
                  }) }}
                  {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                      'text': 'mailvotech.lead.import.runtime',
                      'class': 'col-runtime',
                  }) }}
                  {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                      'text': 'mailvotech.lead.import.progress',
                      'class': 'col-progress',
                  }) }}
                  {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                      'sessionVar': sessionVar,
                      'orderBy': tablePrefix~'.lineCount',
                      'text': 'mailvotech.lead.import.line.count',
                      'class': 'col-line-count',
                  }) }}
                  {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                      'sessionVar': sessionVar,
                      'orderBy': tablePrefix~'.insertedCount',
                      'text': 'mailvotech.lead.import.inserted.count',
                      'class': 'col-inserted-count',
                  }) }}
                  {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                      'sessionVar': sessionVar,
                      'orderBy': tablePrefix~'.updatedCount',
                      'text': 'mailvotech.lead.import.updated.count',
                      'class': 'col-updated-count',
                  }) }}
                  {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                      'sessionVar': sessionVar,
                      'orderBy': tablePrefix~'.ignoredCount',
                      'text': 'mailvotech.lead.import.ignored.count',
                      'class': 'col-ignored-count',
                  }) }}
                  {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                      'sessionVar': sessionVar,
                      'orderBy': tablePrefix~'.createdByUser',
                      'text': 'mailvotech.core.create.by.past.tense',
                      'class': 'col-created visible-md visible-lg',
                  }) }}
                  {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                      'sessionVar': sessionVar,
                      'orderBy': tablePrefix~'.dateAdded',
                      'text': 'mailvotech.core.date.added',
                      'class': 'col-date-added visible-md visible-lg',
                      'default': true,
                  }) }}
                  {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                      'sessionVar': sessionVar,
                      'orderBy': tablePrefix~'.id',
                      'text': 'mailvotech.core.id',
                      'class': 'col-lead-id visible-md visible-lg',
                  }) }}
              </tr>
          </thead>
          <tbody>
            {% for item in items %}
              {{ include('@MailVotechLead/Import/_list_row.html.twig', {
                  'item': item,
                  'permissions': permissions,
                  'indexRoute': indexRoute,
                  'permissionBase': permissionBase,
                  'translationBase': translationBase,
                  'actionRoute': actionRoute,
              }) }}
            {% endfor %}
          </tbody>
      </table>
  </div>
  <div class="panel-footer">
      {{ include('@MailVotechCore/Helper/pagination.html.twig', {
          'totalItems': totalItems,
          'page': page,
          'limit': limit,
          'menuLinkId': indexRoute,
          'baseUrl': path(indexRoute, {'object': app.request.get('object', 'contacts')}),
          'sessionVar': sessionVar,
      }) }}
  </div>
{% else %}
  {{ include('@MailVotechCore/Helper/noresults.html.twig') }}
{% endif %}
