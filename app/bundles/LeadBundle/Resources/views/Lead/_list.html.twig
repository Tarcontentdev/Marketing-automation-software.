{% if items|length > 0 %}
    <div class="table-responsive">
        <table class="table table-hover" id="leadTable">
            <thead>
                <tr>
                    {{ include('@MailVotechCore/Helper/tableheader.html.twig', {
                        'checkall': 'true',
                        'target': '#leadTable',
                    }) }}

                    {% for column, label in columns %}
                        {{ include([('@MailVotechLead/Lead/_list_header_'~column~'.html.twig'), '@MailVotechLead/Lead/_list_header_default.html.twig'], {
                              'label': label,
                              'column': column,
                              'class': (column in columns|keys) ? 'hidden-xs' : '',
                        }) }}
                    {% endfor %}
                </tr>
            </thead>
            <tbody>
                {{ include('@MailVotechLead/Lead/_list_rows.html.twig', {
                    'items': items,
                    'columns': columns,
                    'security': security,
                    'currentList': currentList,
                    'permissions': permissions,
                    'noContactList': noContactList,
                }) }}
            </tbody>
        </table>
    </div>
    <div class="panel-footer">
        {{ include('@MailVotechCore/Helper/pagination.html.twig', {
            'totalItems': totalItems,
            'page': page,
            'limit': limit,
            'menuLinkId': 'mailvotech_contact_index',
            'baseUrl': path('mailvotech_contact_index'),
            'tmpl': indexMode,
            'sessionVar': 'lead',
        }) }}
    </div>
{% else %}
    {% if searchValue is not empty %}
    {{ include('@MailVotechCore/Helper/noresults.html.twig') }}
    {% else %}
    <div class="mt-80 col-md-offset-2 col-lg-offset-3 col-md-8 col-lg-5 height-auto">
        {% set childContainer %}
            <div class="mb-md">
                {% include '@MailVotechCore/Components/pictogram.html.twig' with {
                    'pictogram': 'user--analytics',
                    'size': '80'
                } %}
            </div>

            {{ include('@MailVotechCore/Components/content-item-row.html.twig', {
                type: 'default',
                eyebrow: 'mailvotech.lead.list.eyebrow',
                heading: 'mailvotech.lead.list.heading',
                copy: 'mailvotech.lead.list.copy',
            }) }}
            
            {% set formFeaturesContainer %}
            <div class="row">
                <div class="col-sm-6 col-xs-12">
                    {{ include('@MailVotechCore/Components/content-item.html.twig', {
                        type: 'pictogram',
                        heading: 'mailvotech.lead.list.anonymous.heading',
                        pictogram: 'anonymous--users',
                        copy: 'mailvotech.lead.list.anonymous.copy',
                    }) }}
                </div>
                <div class="col-sm-6 col-xs-12">
                    {{ include('@MailVotechCore/Components/content-item.html.twig', {
                        type: 'pictogram',
                        heading: 'mailvotech.lead.list.known.heading',
                        pictogram: 'id--badge',
                        copy: 'mailvotech.lead.list.known.copy',
                    }) }}
                </div>
            </div>


            {% endset %}

            {{ include('@MailVotechCore/Components/content-group.html.twig', {
                heading: 'mailvotech.lead.list.types.heading',
                childContainer: formFeaturesContainer,
            }) }}
            {% endset %}
            
            {{ include('@MailVotechCore/Components/content-block.html.twig', {
                heading: 'mailvotech.lead.list.block.heading',
                subheading: 'mailvotech.lead.list.block.subheading',
                copy: 'mailvotech.lead.list.block.copy',
                childContainer: childContainer,
                cta: permissions['lead:leads:create'] ? {
                    'label': 'mailvotech.lead.action.add',
                    'link': path('mailvotech_import_action', {'object': 'contacts', 'objectAction': 'new'}),
                    'attributes': {'data-toggle': 'ajax'}
                } : null
            }) }}
    </div>
    {% endif %}
{% endif %}
