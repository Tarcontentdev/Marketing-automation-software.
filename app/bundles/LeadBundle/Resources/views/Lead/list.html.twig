{#
  Variables
    - items
#}
{% set isIndex = 'index' == tmpl ? true : false %}
{% set tmpl = 'list' %}
{% extends isIndex ? '@MailVotechCore/Default/content.html.twig' : '@MailVotechCore/Default/raw_output.html.twig' %}

{% block mailvotechContent %}lead{% endblock %}
{% block headerTitle %}{{ 'mailvotech.lead.leads'|trans }}{% endblock %}

{% block content %}
  {{ include('@MailVotechLead/Lead/_filter.html.twig') }}
  {{ include('@MailVotechLead/Lead/_list.html.twig') }}

  {% if isIndex %}
        </div>
    </div>
    {{ include('@MailVotechCore/Modules/protip.html.twig', {
      tip: random(['mailvotech.protip.contacts.view', 'mailvotech.protip.contacts.tags', 'mailvotech.protip.contacts.list'])
  }) }}
  {% endif %}
{% endblock %}
